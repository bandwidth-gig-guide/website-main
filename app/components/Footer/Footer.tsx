import styles from './Footer.module.css'

const Footer = () => {
    return (
        <div className={styles.wrapper}>
             <footer>
                <p>Footer</p>
            </footer>
        </div>
       
    );
};

export default Footer;
