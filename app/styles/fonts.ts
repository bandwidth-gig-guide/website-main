import { Geist, Geist_Mono } from "next/font/google";

export const font_primary = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const font_secondary = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

